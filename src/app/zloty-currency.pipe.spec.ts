import { ZlotyCurrencyPipe } from './zloty-currency.pipe';

describe('ZlotyCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ZlotyCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
