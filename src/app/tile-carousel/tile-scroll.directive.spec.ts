import { TileScrollDirective } from './tile-scroll.directive';

describe('TileScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new TileScrollDirective();
    expect(directive).toBeTruthy();
  });
});
